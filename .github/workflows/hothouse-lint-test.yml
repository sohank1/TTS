name: "Lint and Test: Hothouse"

on:
    push:
        paths:
            - hothouse/**

jobs:
    ci:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: actions/setup-node@v1
              with:
                  node-version: 14.x
            - run: yarn
            - run: cd ./hothouse
            - run: yarn lint
            - run: yarn build
            - run: yarn test
