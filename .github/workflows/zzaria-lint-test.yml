name: "Lint and Test: Zzaria"

on:
    push:
        paths:
            - zzaria/**

jobs:
    ci:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: actions/setup-node@v1
              with:
                  node-version: 14.x
            - run: |
                  yarn
                  cd ./zzaria
                  yarn lint
                  yarn build
                  yarn test
